import React from "react";
import ReactDOM from "react-dom";
import { InterviewAdminApp } from "./components/InterviewAdminApp";

ReactDOM.render(
  <React.StrictMode>
    <InterviewAdminApp />
  </React.StrictMode>,
  document.getElementById("root")
);
